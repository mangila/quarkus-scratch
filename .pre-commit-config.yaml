---
fail_fast: false
repos:
  - repo: local
    hooks:
      - id: verify-crud1-api-module
        name: Run sub mise task to Maven verify crud1
        entry: mise run //crud1:mvn-test
        language: system
        files: ^crud1/api
        require_serial: true
        pass_filenames: false
