# quarkus-scratch

Just me playing around with Quarkus stuff

Note to myself:

* the root of this project should be python virtual env
* pip install -r requirements.txt
* pre-commit install
* pre-commit install --hook-type pre-push